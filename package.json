{
  "name": "campvus",
  "private": true,
  "version": "1.1.2",
  "description": "Campus P2P content distribution — pnpm workspace root",
  "license": "MIT",
  "packageManager": "pnpm@11.17.0",
  "scripts": {
    "typecheck": "pnpm -r --if-present run typecheck",
    "test": "pnpm -r --if-present run test",
    "dev": "concurrently -n api,web,desktop -c blue,green,magenta \"pnpm --filter @campvus/mode-b-api dev\" \"pnpm --filter @campvus/mode-b-web dev\" \"pnpm --filter campvus-p2p start\"",
    "build": "pnpm --filter @campvus/mode-b-web build && pnpm --filter campvus-p2p build",
    "version:set": "node scripts/set-version.mjs"
  },
  "devDependencies": {
    "concurrently": "^10.0.4",
    "typescript": "^7.0.2"
  }
}
