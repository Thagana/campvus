{
  "name": "@campvus/engine",
  "version": "1.0.0",
  "private": true,
  "description": "Shared P2P content-distribution engine: hashing, signing, manifest model, swarm distribution — used identically by every product mode.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "tsx --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "b4a": "^1.8.1",
    "hyperswarm": "^4.17.0",
    "tweetnacl": "^1.0.3"
  },
  "devDependencies": {
    "@types/b4a": "^1.6.5",
    "@types/node": "^26.1.1",
    "tsx": "^4.23.1",
    "typescript": "^7.0.2"
  }
}
